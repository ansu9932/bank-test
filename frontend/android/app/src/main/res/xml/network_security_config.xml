<?xml version="1.0" encoding="utf-8"?>
<!--
  Banking-grade network policy:
  • Cleartext (HTTP) traffic is blocked app-wide.
  • Only the production API/site domains are reachable; system CAs only, so
    user-installed CAs (mitmproxy/Charles) can NOT intercept traffic.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">alisterbank.online</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
</network-security-config>
